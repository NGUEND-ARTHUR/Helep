# Allow inbound HTTP on port 8004 to notification-service from:
#   nginx ingress controller, prometheus scraper, and other helep pods
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-inbound-notification-service
  namespace: helep
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: notification-service
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - { port: 8004, protocol: TCP }
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
          podSelector:
            matchLabels:
              app.kubernetes.io/name: prometheus
      ports:
        - { port: 8004, protocol: TCP }
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/part-of: helep
      ports:
        - { port: 8004, protocol: TCP }
